import React from "react";

/**
 * @deprecated Reputation metrics have been migrated to the Overview page (/overview).
 * Identity page now focuses purely on public identity registry records.
 */
export const IdentityReputation: React.FC = () => {
  return null;
};
