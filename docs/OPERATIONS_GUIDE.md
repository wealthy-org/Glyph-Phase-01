# Menambahkan Modal Treasury

Untuk menambahkan modal ke Glyph, jalankan perintah berikut di terminal:

```bash
npm run fund:treasury -- --amount 500 --note "Top-up modal riset"
```

Saldo kas Glyph akan langsung bertambah dan otomatis tercatat di Life Log.